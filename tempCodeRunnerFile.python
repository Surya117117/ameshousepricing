import pandas as pd
import pickle

model = pickle.load(open("xgb_model.pkl","rb"))
cols = pickle.load(open("model_columns.pkl","rb"))

df1 = pd.DataFrame([[0]*len(cols)], columns=cols)
df2 = pd.DataFrame([[1]*len(cols)], columns=cols)

print(model.predict(df1))
print(model.predict(df2))